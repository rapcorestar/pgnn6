{
  "name": "pgnn-six",
  "version": "1.0.0",
  "private": true,
  "description": "A looping audiovisual installation about an apartment that remembers its visitor.",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "check": "node --check room.js && node --check panorama.js",
    "build": "npm run check && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.4"
  }
}
